{
  "name": "werner-colors-figma-plugin",
  "version": "1.0.0",
  "description": "Werner's Nomenclature of Colors - Figma Plugin",
  "main": "dist/code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json --outDir dist",
    "watch": "tsc -p tsconfig.json --outDir dist --watch"
  },
  "dependencies": {
    "csv-parse": "^5.5.3"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.82.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3"
  }
}
